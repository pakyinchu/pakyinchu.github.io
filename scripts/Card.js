export class Card {
    constructor(question, answers, category, notes, tags) {
        this.question = question;
        this.answers = answers;
        this.category = category;
        this.notes = notes;
        this.tags = tags;
    }
}
